[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
backend = { path = "../backend" }

[dependencies.libcosmic]
git = "https://github.com/pop-os/libcosmic.git"

features = [
	"winit",
	"wgpu",
]
